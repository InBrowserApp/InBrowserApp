## 什么是 ROT 密码？

ROT（旋转）密码是一种简单的字母替换密码，将每个字母替换为字母表中固定位置后的另一个字母。最著名的变体 ROT13 将字母移位 13 个位置。

例如，`Hello` 使用 ROT13 编码后变成 `Uryyb`。

ROT13 和 ROT47 具有自反性：应用两次相同的算法即可恢复原始文本。这使得它们非常适合隐藏剧透、谜题答案或轻度混淆文本，而无需单独的解密功能。
