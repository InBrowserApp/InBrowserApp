## ROT 暗号とは？

ROT（ローテート）暗号は、各文字をアルファベットの固定位置だけずらした別の文字に置き換える単純な文字置換暗号です。最も有名な変種である ROT13 は、文字を 13 位置シフトします。

例えば、`Hello` は ROT13 でエンコードすると `Uryyb` になります。

ROT13 と ROT47 は自己可逆です：同じアルゴリズムを 2 回適用すると元のテキストに戻ります。これにより、別の復号化関数を必要とせずに、ネタバレ、パズルの答え、または軽度に難読化されたテキストを隠すのに便利です。
