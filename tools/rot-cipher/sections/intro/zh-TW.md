## 什麼是 ROT 密碼？

ROT（旋轉）密碼是一種簡單的字母替換密碼，將每個字母替換為字母表中固定位置後的另一個字母。最著名的變體 ROT13 將字母移位 13 個位置。

例如，`Hello` 使用 ROT13 編碼後變成 `Uryyb`。

ROT13 和 ROT47 具有自反性：套用兩次相同的演算法即可恢復原始文字。這使得它們非常適合隱藏劇透、謎題答案或輕度混淆文字，而無需單獨的解密功能。
