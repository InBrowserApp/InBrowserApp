## ROT 암호란?

ROT(회전) 암호는 각 문자를 알파벳에서 고정된 위치만큼 떨어진 다른 문자로 대체하는 간단한 문자 치환 암호입니다. 가장 유명한 변형인 ROT13은 문자를 13자리 이동합니다.

예를 들어, `Hello`는 ROT13으로 인코딩하면 `Uryyb`가 됩니다.

ROT13과 ROT47은 자기 가역적입니다: 같은 알고리즘을 두 번 적용하면 원래 텍스트로 돌아갑니다. 이로 인해 별도의 복호화 기능 없이 스포일러, 퍼즐 답변 또는 약간 난독화된 텍스트를 숨기는 데 유용합니다.
