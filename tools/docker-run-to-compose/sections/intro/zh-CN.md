## 工具作用

Docker Run 转 Compose 会在浏览器中把一个或多个 `docker run` 命令转换成
`docker-compose.yml` 文件。你可以先检查生成的 YAML，再复制或下载到自己的项目里。

## 适用场景

当你想把一次性的容器启动命令整理成可重复使用的 Compose 服务定义，或者想确认端口、
挂载、环境变量和资源参数会如何映射到 Compose 时，这个工具最合适。

## 使用预期

常见选项如端口、环境变量、重启策略、健康检查、挂载和资源限制都会尽量保留。
不支持或含义不明确的参数会作为警告列出来，方便你在使用前再检查一遍输出。
