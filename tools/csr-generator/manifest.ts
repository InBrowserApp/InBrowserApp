import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "network",
  icon: "lock",
  tags: [
    "csr",
    "pkcs10",
    "x509",
    "certificate",
    "tls",
    "private-key",
    "san",
    "pem",
    "security",
    "offline",
  ],
})
