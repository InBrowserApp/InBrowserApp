## 什么是 IBAN？

IBAN（国际银行账户号码）是用于国际转账的标准化账户标识。

### IBAN 结构

IBAN 以两位国家代码、两位校验位以及国家特定的 BBAN 组成。

### 校验验证

IBAN 使用 ISO 13616 的 mod-97 算法进行校验。

1. 移除空格并将前四个字符移到末尾
2. 将字母转换为数字（A=10，B=11，...，Z=35）
3. 计算 mod 97；有效 IBAN 的余数为 1

每个国家为 BBAN 部分定义固定长度和结构。
