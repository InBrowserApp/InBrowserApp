## Base16(HEX)이란?

Base16(16진수, HEX)은 바이너리를 텍스트로 표현하는 인코딩으로, 0-9와 A-F를 사용해 각 바이트를 두 글자로 나타냅니다. 바이트 확인, 체크섬, 식별자 표현에 널리 쓰입니다.

**일반적인 사용:**

- 디버깅 및 로그에서 원시 바이트 확인
- 해시, 키, ID 표현
- 텍스트만 가능한 환경에서 바이너리 복사/붙여넣기

**참고:**

- Hex는 크기를 두 배로 만듭니다 (1 바이트 -> 2 글자)
- 이 도구는 선택적인 `0x` 접두사를 허용하고 공백을 무시합니다
