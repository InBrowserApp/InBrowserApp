## Base16（Hex）とは？

Base16（16進数/Hex）は、バイナリをテキストに変換するエンコード方式で、0-9 と A-F の文字で各バイトを 2 文字として表します。バイト列の確認、チェックサム、識別子の表現によく使われます。

**主な用途：**

- デバッグやログで生バイトを確認
- ハッシュ、鍵、ID の表現
- テキストしか扱えない環境でのコピー/貼り付け

**注意点：**

- Hex はサイズが 2 倍になります（1 バイト -> 2 文字）
- このツールは `0x` プレフィックスを任意で受け付け、空白を無視します
