import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "crypto",
  icon: "lock",
  tags: [
    "bcrypt",
    "password",
    "hash",
    "security",
    "authentication",
    "salt",
    "offline",
  ],
})
