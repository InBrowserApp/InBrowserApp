import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "hash",
  icon: "lock",
  tags: [
    "bcrypt",
    "password",
    "hash",
    "security",
    "authentication",
    "salt",
    "offline",
  ],
})
