## What is a time zone?

A time zone is a region that shares the same standard time, defined by an offset from UTC (e.g., UTC+08:00). Some regions observe daylight saving time, which shifts the offset seasonally.
