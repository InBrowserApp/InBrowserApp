import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "image",
  icon: "image",
  tags: [
    "favicon",
    "icon",
    "ico",
    "apple-touch-icon",
    "pwa",
    "manifest",
    "web",
  ],
})
