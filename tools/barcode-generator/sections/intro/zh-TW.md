## 這個工具能做什麼

這個條碼產生器可直接在瀏覽器中建立 CODE128、EAN、UPC 等常見條碼格式。你可以
在匯出前調整編碼內容、條寬條高、文字位置與顏色。

## 它有什麼用

當你需要快速產生標籤、庫存編號、包裝樣稿、票券或內部文件用的條碼時，它會很方
便。預覽會即時更新，方便你在下載前檢查間距與可讀性。

## 使用方式

選擇條碼格式，輸入內容，然後調整寬度、高度、邊距、顏色與文字標籤設定。預覽滿意
後，可下載為 PNG 或 SVG。
