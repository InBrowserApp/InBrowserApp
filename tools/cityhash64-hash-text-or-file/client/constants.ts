const DEFAULT_TEXT = "Hello from CityHash64 in the browser."
const DEFAULT_SEED = ""

const STORAGE_KEYS = {
  text: "tools:cityhash64-hash-text-or-file:text",
} as const

export { DEFAULT_SEED, DEFAULT_TEXT, STORAGE_KEYS }
