## 什么是端口号？

端口号是一个 16 位无符号整数 (0-65535)，用于标识网络设备上的特定进程或服务。端口允许多个网络服务在同一 IP 地址上运行。

### 系统端口 (0-1023)

也称为"知名端口"，保留用于常见服务，如 HTTP (80)、HTTPS (443)、SSH (22)。

### 注册端口 (1024-49151)

由在 IANA 注册的应用程序和服务使用，如 MySQL (3306)、PostgreSQL (5432)。

### 动态端口 (49152-65535)

也称为"临时端口"，由客户端应用程序用于临时连接。
