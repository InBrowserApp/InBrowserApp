## 什麼是 JSON Schema 驗證？

當您在設計資料負載、除錯 API 範例，或檢查結構描述變更是否破壞範例資料時，可以使用此工具獲得快速回饋。所有作業都在瀏覽器本機執行，原始 JSON 永遠不會離開頁面。

## 適合的使用情境

- 檢閱 API 文件中的範例負載。
- 在前端開發期間驗證模擬資料。
- 檢查對格式敏感的欄位，例如 `uuid`、`email` 或 `date-time`。

## 它無法取代的事項

- 伺服器端的授權與業務規則。
- 依賴遠端參考或應用程式狀態的合約檢查。
- 針對整套結構描述的完整 CI 驗證。
