import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "crypto",
  icon: "lock",
  tags: [
    "bcrypt",
    "hash",
    "password",
    "verify",
    "authentication",
    "security",
    "crypto",
  ],
})
