import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "network",
  icon: "network",
  tags: ["network", "ip", "dns", "cidr", "http", "port", "tls", "ssh", "tools"],
})
