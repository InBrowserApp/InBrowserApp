## 什麼是 PBKDF2？

PBKDF2（基於密碼的金鑰派生函式 2）使用鹽值與多次迭代從密碼派生加密金鑰。它可減緩暴力破解，並在鹽值改變時產生不同的金鑰。

**要點：**

- 使用 HMAC + 指定雜湊（SHA-1/SHA-256 等）
- 迭代次數越高，計算成本越高
- 輸出長度可設定

**最佳實務：**

- 為每個密碼使用唯一且隨機的鹽
- 在可接受效能內提高迭代次數
- 新系統可考慮 Argon2 或 scrypt 等記憶體困難型 KDF
