## 如何将 IPv6 转换为 MAC 地址

当 IPv6 地址是使用 EUI-64 格式从 MAC 地址生成时，可以将 IPv6 地址转换为 MAC 地址。这通常适用于 IPv6 链路本地地址（以 fe80:: 开头）和一些无状态自动配置地址。转换过程包括：1）从 IPv6 地址中提取接口标识符（最后 64 位），2）反转第一个字节的第 7 位（通用/本地位），3）移除插入的 'fffe' 字节以重构原始的 48 位 MAC 地址。请注意，这只适用于最初使用 EUI-64 从 MAC 地址派生的 IPv6 地址。
