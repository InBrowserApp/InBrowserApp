## 如何將 IPv6 轉換為 MAC 位址

當 IPv6 位址是使用 EUI-64 格式從 MAC 位址生成時，可以將 IPv6 位址轉換為 MAC 位址。這通常適用於 IPv6 鏈路本地位址（以 fe80:: 開頭）和一些無狀態自動配置位址。轉換過程包括：1）從 IPv6 位址中提取介面識別符（最後 64 位），2）反轉第一個位元組的第 7 位（通用/本地位），3）移除插入的 'fffe' 位元組以重構原始的 48 位 MAC 位址。請注意，這只適用於最初使用 EUI-64 從 MAC 位址派生的 IPv6 位址。
