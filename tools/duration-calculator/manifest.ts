import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "time",
  icon: "globe",
  tags: ["time", "duration", "calculator", "iso-8601", "timezone", "timestamp"],
})
