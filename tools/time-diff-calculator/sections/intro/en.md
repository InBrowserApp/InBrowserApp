## What is a time difference?

A time difference is the elapsed time between two timestamps. Comparing local times requires their time zones because daylight saving can change offsets, so converting to UTC keeps the comparison unambiguous.
