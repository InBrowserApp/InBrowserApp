## 这个工具适合做什么

当你需要把有效 TOML 转成更容易阅读、分享或提交的 JSON 时，可以使用这个工具。所有处理都在浏览器本地完成。

## 适合哪些场景

- 把 API 示例转换成适合文档或配置文件的 JSON。
- 将复制来的 TOML 载荷改写成更容易人工对比的格式。
- 为要求 JSON 的工具和工作流准备结构化内容。

## 需要注意什么

- 这个工具里的输入必须是合法 TOML。
- 转换器会保留数据结构，但 JSON 的呈现方式仍可能和手写风格不同。
- 如果你要反向转换，请使用 JSON 转 TOML 工具。
