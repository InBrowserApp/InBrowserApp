import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "image",
  icon: "image",
  tags: ["image", "palette", "color", "extractor", "dominant", "css", "design"],
})
