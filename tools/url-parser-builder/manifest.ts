import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "web",
  icon: "network",
  tags: [
    "url",
    "uri",
    "parser",
    "builder",
    "hostname",
    "query",
    "fragment",
    "web",
  ],
})
