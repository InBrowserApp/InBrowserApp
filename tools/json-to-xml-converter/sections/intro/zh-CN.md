## 这个工具适合做什么

当你需要把有效 JSON 转成更容易阅读、分享或提交的 XML 时，可以使用这个工具。所有处理都在浏览器本地完成。

## 适合哪些场景

- 把 API 示例转换成适合文档或配置文件的 XML。
- 将复制来的 JSON 载荷改写成更容易人工对比的格式。
- 为要求 XML 的工具和工作流准备结构化内容。

## 需要注意什么

- 这个工具里的输入必须是合法 JSON。
- 转换器会保留数据结构，但 XML 的呈现方式仍可能和手写风格不同。
- 如果你要反向转换，请使用 XML 转 JSON 工具。
