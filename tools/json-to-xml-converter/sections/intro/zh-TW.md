## 這個工具適合做什麼

當你需要把有效 JSON 轉成更容易閱讀、分享或提交的 XML 時，可以使用這個工具。所有處理都會在瀏覽器本機完成。

## 適合哪些場景

- 把 API 範例轉成適合文件或設定檔的 XML。
- 將複製來的 JSON 負載改寫成更容易人工比對的格式。
- 為要求 XML 的工具與工作流程準備結構化內容。

## 需要注意什麼

- 這個工具中的輸入必須是合法 JSON。
- 轉換器會保留資料結構，但 XML 的呈現方式仍可能和手寫風格不同。
- 如果你要反向轉換，請使用 XML 轉 JSON 工具。
