import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "crypto",
  icon: "lock",
  tags: [
    "hash",
    "argon2",
    "password",
    "verification",
    "security",
    "crypto",
    "authentication",
  ],
})
