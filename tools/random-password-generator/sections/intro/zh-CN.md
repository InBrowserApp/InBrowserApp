在浏览器本地生成随机密码、口令短语、分组代码和数字 PIN，不需要把结果发送到外部服务。

## 选择合适的格式

使用 **Random** 生成高强度密码，使用 **Words** 生成更适合口述或手动输入的口令短语，使用 **Blocks** 生成分组代码，使用 **PIN** 生成短数字凭证。

## 调整输出

你可以控制字符集、分隔符、分组方式，以及是否排除容易混淆的字符，让结果更贴合真实使用场景。

## 导出与分享

确认结果后可以直接复制或下载为文本文件。需要临时遮挡屏幕内容时，也可以先隐藏结果，再继续保留当前设置。
