import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "random",
  icon: "lock",
  tags: [
    "random",
    "password",
    "passphrase",
    "pin",
    "security",
    "generator",
    "bip39",
  ],
})
