## 什么是 Base16（Hex）？

Base16，也叫十六进制（Hex），是一种二进制到文本的编码方式，用 0-9 和 A-F 表示数据，每个字节对应两个字符。它常用于查看字节数据、校验和以及标识符等场景。

**常见用途：**

- 调试和日志中查看原始字节
- 表示哈希、密钥和 ID
- 在只支持文本的场景中复制/粘贴二进制数据

**注意：**

- Hex 会让数据体积翻倍（1 字节 -> 2 字符）
- 本工具支持可选的 `0x` 前缀并忽略空白字符
