## 这个工具适合做什么

使用这个工具可以把压缩或混乱的 JSON 整理成清晰易读的格式，再粘贴到文档、
测试、工单或 PR 中。所有处理都在浏览器本地完成。

## 适合哪些场景

- 检查 API 请求和响应载荷。
- 在分享前重新格式化导出的 JSON 文件。
- 在不修改数据的前提下阅读层级很深的对象和数组。

## 需要注意什么

- 该格式化器只接受合法的 JSON。
- 注释、尾随逗号以及其他只属于 JavaScript 的语法都会失败。
- 格式化只会调整空白和缩进，不会重排键名或修改值。
