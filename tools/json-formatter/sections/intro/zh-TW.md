## 這個工具適合做什麼

使用這個工具可以把壓縮或凌亂的 JSON 整理成清楚易讀的格式，再貼到文件、
測試、工單或 PR 中。所有處理都會在瀏覽器本機完成。

## 適合哪些場景

- 檢查 API 請求和回應負載。
- 在分享前重新格式化匯出的 JSON 檔案。
- 在不修改資料的前提下閱讀層級很深的物件和陣列。

## 需要注意什麼

- 這個格式化工具只接受合法的 JSON。
- 註解、尾隨逗號以及其他只屬於 JavaScript 的語法都會失敗。
- 格式化只會調整空白和縮排，不會重排鍵名或修改值。
