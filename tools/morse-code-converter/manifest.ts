import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "misc",
  icon: "file-text",
  tags: [
    "morse",
    "morse code",
    "text converter",
    "encoder",
    "decoder",
    "audio",
  ],
})
