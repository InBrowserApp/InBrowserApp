## 這個工具能做什麼

從螢幕或圖片取色，取樣像素並即時複製 HEX、RGB、HSL、HSV 與 CMYK 值。

## 取色建議

- 當瀏覽器支援 EyeDropper，且你要從其他應用程式、分頁或設計參考取色時，優先使用螢幕取色。
- 當你要從截圖、Logo、產品照片或介面稿中擷取精確像素時，使用圖片取色。

## 輸出說明

- 當你需要 `#RRGGBBAA`、`rgba(...)`、`hsla(...)` 或 `hsva(...)` 這類含透明度的格式時，開啟 Alpha。
- 在網頁與介面工作中，HEX 與 RGB 通常最方便；如果要交付印刷流程，CMYK 會更合適。
