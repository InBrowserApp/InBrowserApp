## What is JSON Schema?

JSON Schema is a standard for describing the shape of JSON data. It lets you define required fields, data types, ranges, and format rules so data can be validated consistently.

**Common uses**

- Validate API request and response payloads
- Enforce configuration file structure
- Document data contracts across teams
