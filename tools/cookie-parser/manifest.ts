import { defineTool } from "@workspace/tool-sdk"

export const tool = defineTool({
  category: "web",
  icon: "network",
  tags: [
    "cookie",
    "set-cookie",
    "header",
    "http",
    "parser",
    "json",
    "browser",
    "security",
  ],
})
