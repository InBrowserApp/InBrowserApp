Base64 适合在“只能传文本”的通道里承载原本更适合二进制的数据，比如邮件正文、JSON 字段或小型 data URL。它只是编码层，不是安全层。

## 什么时候适合用

- 调试接口时，快速查看 API 返回的 Base64 字符串。
- 把浏览器里的文本转换成更适合传输或记录日志的格式。
- 检查一段 Base64 内容究竟会解码成什么文本。

## 需要注意什么

- Base64 通常会让体积增加约三分之一。
- 它不会加密，也不会隐藏原始内容。
- 如果补位不完整或复制过程断裂，通常就会出现解码错误。
