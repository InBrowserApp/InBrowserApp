## 什麼是 Base64？

當基於文字的通道需要傳送對二進位友善的內容時，Base64 就派上用場了，例如電子郵件內容、JSON 區塊或小型 data URL。它只是一層編碼，並非安全機制。

## 何時使用

- 當 API 回傳或需要 Base64 字串時，用於快速除錯。
- 將瀏覽器中的文字轉換為安全的傳輸格式，用於記錄檔或負載資料。
- 檢查貼上的 Base64 內容是否能解碼成您預期的結果。

## 需要留意的事項

- Base64 會使資料大小增加約三分之一。
- 它並不會加密或隱藏原始內容。
- 填充錯誤或複製貼上不完整通常會以解碼錯誤的形式出現。
