---
import BaseLayout from '../../layouts/BaseLayout.astro'
import { toolRegistry } from '@inbrowserapp/tools/registry'
import { translateMeta } from '@inbrowserapp/tools/i18n'

const lang = 'en'
---

<BaseLayout title="All Tools" description="Browse all available online browser tools" lang={lang}>
  <div class="max-w-6xl mx-auto p-8">
    <h1 class="text-4xl font-bold mb-2 text-gray-900 dark:text-gray-100">All Tools</h1>
    <p class="text-lg text-gray-600 dark:text-gray-400 mb-12">
      Free online browser tools - Privacy focused, no data collection
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        toolRegistry.map((tool) => {
          const meta = translateMeta(tool, lang)
          return (
            <a
              href={`/tools/${tool.id}`}
              class="block p-6 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg no-underline text-inherit transition-all hover:border-blue-500 hover:shadow-lg hover:shadow-blue-500/10 hover:-translate-y-0.5"
            >
              <h3 class="text-xl font-semibold mb-2 text-gray-800 dark:text-gray-100">
                {meta.name}
              </h3>
              <p class="text-sm text-gray-500 dark:text-gray-400 mb-4 leading-relaxed">
                {meta.description}
              </p>
              <div class="flex flex-wrap gap-2">
                {tool.tags.slice(0, 3).map((tag) => (
                  <span class="inline-block px-3 py-1 bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 text-xs rounded-full">
                    {tag}
                  </span>
                ))}
              </div>
            </a>
          )
        })
      }
    </div>
  </div>
</BaseLayout>
