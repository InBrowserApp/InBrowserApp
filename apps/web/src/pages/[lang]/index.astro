---
import HomePage from "@/components/pages/home-page.astro"
import { resolveSiteLanguage } from "@/lib/site"
import { getNonDefaultSiteStaticPaths } from "@/lib/static-paths"

export { getNonDefaultSiteStaticPaths as getStaticPaths }

const language = resolveSiteLanguage(Astro.params.lang)
---

<HomePage language={language} />
